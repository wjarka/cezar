# Mobile new-task reachability diagnosis

Reported offscreen44px execution control is in normal document flow, not clipped. On360x640/light/comfortable, variants starts at y771–815 with44px height and48.59px width. After scrolling the actual main scroller, it is at y324–368; elementFromPoint hits the button, and a real agent-browser click opens its menu. Base branch also opens by real click after scrolling. Start task is enabled and hit-testable at48px height. All12 combinations of360x640/390x844,light/dark,and comfortable/compact/ultra pass. Screenshots show before-scroll and after-scroll/click; proof.json records every rectangle and click outcome. Actual built asset SHA is recorded; no app source fix is warranted for this offscreen condition.

For verifier86: keep44px dimension checks and horizontal clipping checks. For offscreen controls, scrollIntoView({block:"center",inline:"nearest"}), await settled layout/focus restoration, then require viewport intersection plus elementFromPoint target containment and a real menu-opening click. Do not equate checkVisibility() with intersection with the viewport, or require all execution settings to fit the first viewport. Escape restores focus asynchronously; wait until the previous popup closes and scrolling settles before measuring the next target. E2E expectation changes remain86-owned.

Log qualification: the supplied e2e-failures.log has new-task failures for removed twinkle-backdrop and textarea covered by an open Radix popup; it does not include an offscreen44px new-task assertion or a new-task-hierarchy failure. Those recorded failures must not be silently reclassified by this proof. The script reproduces the separately reported offscreen execution-control condition and verifies reachability explicitly;86 should associate it with the current failing case and rerun that assertion.

Run from the worker root against isolated fixture44636: python3 .ai/design-reference/iteration-3/newtask-session/mobile-reachability/reproduce.py. No external task, credential, real agent execution, or e2e source was modified.
