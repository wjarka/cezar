# Combined Git/GitHub review

This is the latest comparison against confirmed 193-frame source `56a71a27c7795137cab1c0a40a5839edecbacab4014d2d39ddc90f89343275f8`. The disposable source snapshot combines parent shared shell/task headers at `4d7723df80b5e41b3c45f62b986b75decd488812` with the 37 owned Git/GitHub files from `901f75af81dbb7877bb6849ba0ced29393f4af5b`. Its local snapshot commit is `e4b19a4b20732a174da28e33cc604ae1648f0a6b`. No parent merge or parent working-tree modification occurred.

The combined server and web builds, web typecheck, and all 272 scoped tests passed. `build-proof.json` pins built assets; `server-proof.json` verifies every one of the 90 served assets. `../combined-source-proof.json` pins each overlaid source file. The populated fixture reuses the parent six-file runtime infrastructure and validates all 15 adapted Git/GitHub responses and three readiness states against the built contract. Readiness screenshots explicitly assert their DOM eligibility before capture. Duplicate parent exact-URL routes were removed before installing the complete fixture.

`manifest.json` contains 44 actual reference/browser pairs. `interaction-manifest.json` and `menu-manifest.json` contain 32 additional contextual comparisons across both themes and desktop/mobile. `r2-resize-manifest.json` contains six additional captures reached by pointer drag beyond 280/520 bounds or double-click reset to 360, with reload persistence and keyboard Home/End/ArrowLeft checked. No real merge, task launch, branch switch, commit or external write was submitted.

The exported design PNG dimensions govern CSS viewport (2x design versus 1x browser). Several mobile PNGs extend beyond their nominal JSON frame height; captures use the actual exported height, rather than asserting equality with stale nominal dimensions. Raw PNGs are untouched. Contact sheets normalize only for human review.

Per-pair notes describe remaining discrepancies. Full hierarchical diffs, commit author/time/SHA/search, direct-path lazy file opening, exact-head readiness details, and real account/model/workflow handoff choices remain available. The source omits or abbreviates these controls. Its PR assignee/board filters, log PR associations and handoff sync timestamp are unsupported by the current API and are not fabricated. The blocked source board combines confirmation and conflict states; runtime preserves the real modal confirmation and mutually exclusive conflict gates. The populated sidebar has one project and four linked tasks, whereas the reference has multiple projects and pinned tasks. These are reviewed differences, not a whole-screen pixel-match claim.

Replay: create a fresh disposable snapshot using `../combined-build.py`, build server/web there, then follow its copied `.ai/qa/runtime-verified/README.md` on unique port 44663/session git5653-combined. Install `fixture.py`, run `capture.py`, `interactions.py`, `menus.py`, `finalize-evidence.py`, then `r2-resize.py`; generate contact sheets with Pillow. The parent reference export path can be supplied using CEZ_DESIGN_REFERENCE. Snapshot identity and fixture paths must be recorded anew when replayed.

Adapter follow-up: all mapped route icons now import the shared adapter, including Columns2. The former local 23-glyph copy and duplicate license were removed; explicit size=16 preserves prior route sizing. Unmatched utility glyphs remain Lucide. The source align-left fallback is not used as an alignment icon. Final captures were refreshed after this switch.
