# Independent acceptance notes (in progress)

Reference ledger has193unique IDs; historical baseline rows are not final evidence. Current route fixture baseline b87b5082 and served86asset hashes verified. Settings later seeded through real isolated APIs; account/skill installation status mocked with contract validation. Fixture mutations do not touch user homes or real projects.

## Findings reviewed so far

- New Task desktop/mobile: source hierarchy and purple/yellow control treatment present. Retained starters/follow-ups extend page versus1A–D; newer23A explicitly requires these, so do not remove them to force the old viewport. Actual auto model and branch differ legitimately. Mobile Start sits below first viewport because retained starters add height; scrolling must stay reachable. Shared desktop header height differs.
- Session: genuine task transcript/worker data differs; no historical per-turn runner/model fabricated. Default narrow capture was rejected and wide recaptured. Session tab uses black active accent, reference purple; record as visual mismatch. Current workflow/progress and document-flow composer remain. Mobile must be judged from top-scrolled capture, not initial bottom anchor.
- Project Tasks: structural table/cards match; retained Mark all read, Columns, pins, child rows and comparison strip alter height. Row counts/statuses/PR data differ legitimately. Mobile resource table and floating New task need separate state review.
- All Tasks: initial empty capture rejected as populated acceptance; isolated worktree registration is suppressed. Refreshed with schema-valid two-project registry/index fixtures; no fabricated diffStat, known global API limitation stays explicit.
- Git Changes: no-remote disables Pull honestly; one dirty README differs from9reference files. Structured code diff is actual API data. Awaited settled recapture replaces transient loading shots. Mobile hides split controls by existing behavior; extra inner totals remain.
- Git Commits/Branches: actual three commits/branches versus design’s longer lists. Real author/time/SHA retained (reference list omits some metadata); new-branch form preserves actual base semantics. No remote call performed.
- GitHub: current response fixtures missing PR assignee/board data cannot justify inventing controls. Issue selection and PR readiness composite require separate captures. PR unknown-requirements remains blocked despite passing checks. Mobile list-only initial route is not accepted as selected-detail counterpart.
- Inbox: original fixture has one runnable card, so note-only card and expanded instructions state must be added as contract fixtures before acceptance. Actual runner/model/effort retains more controls.
- Automations list: layout closely tracks reference; initial recent activity empty because list/log payloads differ—must capture populated activity. Editor matches three panels but true horizontal overflow from hidden legend is blocking.
- Variants: cards/diff accordions structurally match. Actual review/done statuses and resource totals retained; no progress notes in fixture is legitimate data difference. Waiting/error/pick confirmation need separate checks.

## Deterministic regression

Offline default-project fallback spins at scope-resolving (routes.test.tsx isolated repro); source owner requested. Two stale test expectations fixed locally: grouped diff digits and guardian View changes action allowance. No behavior tests removed.

## Settings interiors — independent current seeded pair review

All S01–S13 desktop-light and mobile-light pairs inspected directly, with matching content selection for MCP and real browser Blocked notification permission. General/global index rhythm is close. Explicit visual departures remain:

- S01 General includes added/last-opened registry facts and longer actual paths; boot-project removal correctly disabled. Extra height is actual information, not a missing control.
- S02 Agents has personal-account warning, extra provider/model disclosures, native selects, and additional base help. Save prompt is yellow rather than reference purple. Live-title/review switch thumbs appear absent in these captures (check scoped toggle implementation/shared primitive).
- S03 Agent config retains provider tabs plus actual catalog selection rather than three fixed file tabs. Scope/precedence/commit-effect instructions remain. Code editor syntax coloring differs and Save/Reset order/color differs. Actual byte-exact Save+Reset smoke passed.
- S04 Worktrees retains explicit per-row deletion, disk size/age, longer reclaim explanation and inline retention Save; reference has shorter summary and vertical field/action. All extra controls must stay, but layout is visibly taller.
- S05 Bookmarklets uses actual local+global catalog; count/paths differ. Missing per-row source labels and bold Run from GitHub heading differ. Copy/drag help retained. No launcher executed.
- S06 Templates retains list-level Save/Reset, separate chips and apply-with picker; reference shows per-template Save and combined chip field. Must not change persistence semantics to match art.
- S07 General global index matches section hierarchy with additional wording/wrapping and smaller stored-locally badge.
- S08 Appearance intentionally supports only Cezarion accent; Lime/Violet sample choices not fabricated. Mobile theme/width choices stack full-width rather than reference inline rows. Preview retained; density/width behavior covered separately by tests.
- S09 Notifications presents genuine denied state; mobile toggle right-aligned rather than centered below description. Text wrapping/panel height differ; no notification sent.
- S10 Resources uses real selects, individual Save controls and three-state inherited composer defaults. Reference shows plain fields/two toggles/combined Save. Resulting long mobile panel remains scrollable; known semantic difference, not exact parity.
- S11 Skills settings reports schema-fixtured six current skills, with inheritance disclosure/On label and right-aligned mobile toggle. Source values are disclosed fixtures, not actual installed skill claims.
- S12 Accounts retains provider tabs, per-account details/privacy controls and per-agent defaults. Reference simultaneous Codex+Claude+authentication-in-progress is not reproduced; no synthetic active auth flow. Large explicit parity gap, auth capabilities remain tested.
- S13 Projects retains independently saved folders, help, real registry timestamps/tags, limits and boot removal guard; reference shorter combined Save folders layout. Mobile Add project sits later in document flow and is reachable (real dialog smoke passed).

No Settings behavior test for auth, MCP, scope or persistence was deleted by d6573fcf..b87b5082; removed cases concern replaced horizontal-nav overflow, replaced with disclosure/deep-link checks. Existing separate suites retain auth, hosted read-only, scope switching, byte save, project/global payload and appearance round-trips.

## Integrated final findings

- Both owned runtime regressions fixed in8ec8fdbc; stable full five repository gates pass (386files/7999Vitest,261nodeunit,27package). These claims do not include browser acceptance.
- Shared mobile drawer initial snapshot caught an in-flight transform and was rejected; recapture after1200ms shows complete334px drawer at402px and visible68px dismissal strip. Navigation screenshot is now settled.
- Plan desktop remains an overlay dialog over New Task; reference depicts an embedded canvas. Mobile action bar stays at bottom with blank space above when fewer steps exist. Three schema-fixtured steps are legitimate data, layout difference is explicit.
- All Tasks populated grouping/tag/actions implementation is materially taller than5A–D's table;26A captures require grouped cards and cannot make both shapes an exact match. No global diff values fabricated.
- Tools light/dark hierarchy matches its two sections. Local long path and six actual CLI statuses differ from short sample folder/four tool statuses; no remote repository row is fabricated. Source instructional footer is not user-facing behavior. Current Tools menu keeps setup/diagnostics/settings and actual tool versions.
- Current Inbox now has both runnable and note-only fixtures, and expanded instructions. Dark mobile retains separate effort control and stacked actions. Automation activity now populated through correctautomation-log endpoint; displays timestamps and extra spacing versus reference, genuine schema fixture only.
- GitHub ready and unknown states are captured separately with contract-valid requirements. Ready enables merge; unknown stays blocked. No merge clicked. Reference27B bypass/confirmation/conflict composite still lacks complete independent acceptance.
- Some e2e target failures are invalid bounding-box assumptions: mobile menu has20px layout but44px pseudo-element hit area; edge hit-testing verified. Others remain unclassified until stable full output; never classify the whole suite as stale.

## Final bundle supersedes earlier checkpoint findings
All146captured pairs inspected; all52Settingsfinal states inspected. S02thumbs restored; S03file scope and S12account identity/count corrections verified. Final22NewTask/Session/Changes/Commits/Files captures reinspected: purpletabsrestored, truefixturefivefiles includes realMCP/configsmoke; dynamicdata doesnotestablish oldreferencepixelparity. NewTaskdarkthumbcorrection andViewYAML44px fix now underway; source23geometryclose, legacy1mobileCTAinitialviewportstilldifferent. Remaining API/state/contrastgaps explicit in ACCEPTANCE.md.
